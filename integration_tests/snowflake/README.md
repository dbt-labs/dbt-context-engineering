# integration_tests / snowflake

Runnable dbt project targeting snowflake. The cloud AI calls (`ce_generate`, `ce_classify`,
`ce_extract`, `ce_embed`, `ce_vector_search`) have executed successfully here against mock sample
data; not yet validated against real production data at scale.
