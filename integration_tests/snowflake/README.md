# integration_tests / snowflake

Runnable dbt project targeting snowflake. The cloud AI calls (`generate`, `classify`,
`extract`, `embed`, `vector_search`) have executed successfully here against mock sample
data; not yet validated against real production data at scale.
